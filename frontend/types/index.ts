export interface Product {
    id: number
    name: string
    dataCategory: string
    recordCount: number
    fields: string[]
    description?: string
  }
  
  export interface User {
    id: number
    email: string
    name: string
  }
  
  